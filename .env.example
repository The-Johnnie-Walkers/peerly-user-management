
# Puerto del servidor HTTP
PORT=3001

# MongoDB
DB_URI=mongodb://localhost:27017/peerly

# CORS — orígenes permitidos (separados por coma)
CORS_ORIGIN=http://localhost:5173

# Los avatares se guardan como ruta relativa (/uploads/avatars/...).
# El frontend las resuelve con VITE_USER_MGMT_URL al mostrarlas.
